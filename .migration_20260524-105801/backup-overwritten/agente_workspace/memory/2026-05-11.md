## Objetivo: Instalar y configurar la skill Himalaya para OpenClaw en sistema Linux bare metal.
## Estado: En Progreso (Falla en instalación interactiva de Smithery).
## Contexto: El asistente Kronos no puede resolver prompts interactivos de `npx smithery`.
## Acciones pendientes:
1.  **Obtención de permisos/manuales:** El usuario (Manoel) debe ejecutar la instalación manualmente o proporcionar una ruta de instalación no interactiva.
2.  **Análisis de error:** El comando `npx -y smithery skill add openclaw/himalaya` falló probablemente por falta de interacción en el prompt de selección de agentes.
3.  **Alternativa de instalación:** Buscar si hay un binario pre-compilado o una forma de instalarlo vía `cargo` (Rust) o `apk/yum/dnf` (Linux package manager).
4.  **Backup:** Antes de intentar comandos alternativos, se debe asegurarse de no sobrescribir el entorno global de Smithery si se rompe.
5.  **Próximos pasos:**
    -   Sugerir al usuario ejecutar: `npx -y smithery@latest install openclaw/himalaya` manualmente.
    -   Verificar si `himalaya` es un paquete de Rust y, de ser así, intentar `cargo install himalaya`.
    -   Si falla, intentar instalar desde una URL específica de GitHub releases si está disponible.
6.  **Configuración posterior:**
    -   Crear `~/.config/himalaya/config.toml`.
    -   Validar con `himalaya --version`.
7.  **Registro en memoria:** Guardar el estado actual y el error encontrado para referencia futura.
